import React from 'react';

function NavBar() {

    let styles = {
        color:'red',
        fontSize:'30px'
    }

    return(<div>
        <p style={styles}>lorem</p>
      </div>);
}

export default NavBar;
